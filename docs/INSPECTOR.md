# MCP Inspector

Attempted on 2026-07-26:

```powershell
npx @modelcontextprotocol/inspector --version
```

The command timed out after 124 seconds. No Inspector evidence is claimed for this branch. SDK-level STDIO and Streamable HTTP behavior was validated with `McpExamples.Client.Console`, which uses the official C# SDK client transports.
