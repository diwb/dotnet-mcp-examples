# Contributing

Use small, focused pull requests. Run `dotnet restore`, `dotnet build -c Release`, and `dotnet test -c Release` before submitting changes. Do not add examples that require paid APIs, secrets, unrestricted shell execution, arbitrary SQL, or filesystem access outside the repository sandbox.
